{
  "name": "@lich/lich",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "bin": {
    "lich": "./dist/lich"
  },
  "scripts": {
    "build": "bun build --compile --target=bun --outfile=dist/lich src/bin/lich.ts",
    "dev": "bun run src/bin/lich.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  },
  "dependencies": {
    "mri": "^1.2.0"
  }
}
