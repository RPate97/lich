{
  "name": "@levelzero/plugin-infisical",
  "version": "0.1.0",
  "description": "Levelzero plugin contributing a bulk EnvSource that fetches secrets from Infisical (machine-identity / service-token auth) and re-publishes them under the `infisical` namespace. Pairs with `@levelzero/plugin-dotenv`, which loads the credentials at boot.",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "@levelzero/core": "workspace:*"
  },
  "dependencies": {
    "@infisical/sdk": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.0",
    "bun-types": "^1.2.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
