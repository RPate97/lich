# Warm-Fork V0 Benchmark

- Host: darwin arm64, MacBook-Pro-5.local
- Substrate: Tart on macOS Virtualization.framework
- Image: lich-sandbox-base
- Stack: dogfood-stack dev:heavy (postgres + 500 synthetic migrations)
- Warm runs: 3

## Headline

| Metric        | Time |
|---------------|------|
| Cold boot     | 62.48s |
| Avg warm-fork | 13.81s |
| **Speedup**   | **4.5x** |

## Raw timings

- Cold: 62478ms
- Warm[1]: 13976ms
- Warm[2]: 13700ms
- Warm[3]: 13758ms

## Method

1. Copy dogfood-stack fixture to a fresh tmp dir.
2. Generate 500 synthetic migrations via fixtures/dogfood-stack/scripts/generate-heavy-migrations.sh.
3. Inject `runtime.sandbox` block (backend: tart, image: lich-sandbox-base, warm_fork: true).
4. `lich sandbox purge` + `lich down --purge` to wipe any prior state.
5. Time `lich up dev:heavy` (cold-boot path). VM gets created, source synced in, migrations applied, snapshot taken on success.
6. Repeat 3 times: `lich down --purge` (drops run VM, keeps snapshot), then time `lich up dev:heavy` (warm-fork path: clones snapshot, resumes).

Each `lich up` invocation is wall-clock timed from CLI invocation to exit.
